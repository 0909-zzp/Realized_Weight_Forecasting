# 2026-06-23

- 完成 `纯权重计算.py` 性能瓶颈分析：graphical_lasso 占 85%+ 运行时间（本质算力问题，非代码逻辑问题）。三个可修复瓶颈：raw_cov.copy() 全量拷贝、prec@ones 双重计算、eigvalsh 每5天调用。
- 输出 `纯权重计算_优化版.py`：in-place Ridge（省 1.2MB/天拷贝）、单次矩阵-向量乘积、条件数降为每40天抽样、移除 degree 大数组序列化。预期提速 10-15%。
- 设计云端算力方案：
  - `_cloud_bootstrap.sh` — 单VM一键部署（pip→检查数据→运行→验证输出）
  - `_cloud_data_pack.py` — 本地打包（整包 ~2.9GB 或分块并行上传）
  - `_cloud_dispatcher.py` — 多VM分布式调度手册（数据分片→分发→并行计算→回传→合并）
  - `_cloud_merge.py` — 结果合并脚本（去重+完整性+NaN检查+统计摘要）
- 推荐策略：先用 `纯权重计算_优化版.py` 本地跑（12核 ~8-15分钟），若不满足再走云端方案。
- **发现 BLAS 线程风暴根因**：NumPy 的 OpenBLAS 编译配置 `MAX_THREADS=24 + NO_AFFINITY` 导致多进程×多线程双层并行互相踩踏。11 worker 各起 BLAS 多线程 → 264 线程抢 8 核 → 性能退化 40~60×（2436天从 15min → 63h）。修复：在 import numpy 前设 5 个环境变量强制 BLAS 单线程。
- 云端 AutoDL 验证（RTX 2080Ti + Xeon 8255C）：单天 36s，但 Xeon 单核弱+网络存储慢，不稳定。结论：本地消费级 CPU 优于服务器 CPU。
- 旧进程（步骤二.py）跑完 63h 产出 `reg_weights_2436.csv`，数据验证通过：2436×392，0 NaN，权重和=1.0。
- 文档化：README §4.4 新增排查条目、规范.mdc §七 新增坑点 #13。
