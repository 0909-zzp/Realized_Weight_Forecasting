# 2026-06-24

- 旧进程（步骤二.py，63h）完成产出 `reg_weights_2436.csv`，验证通过：2436×392，0 NaN，权重和=1.0。
- 完成 BLAS 线程风暴根因文档化：README §4.4 新增排查条目、规范.mdc §七 新增坑点 #13、MEMORY.md 记录为技术决策。
- 审查描述性统计 Table1：权重截面均值=0.002551(=1/392) ✓；截面标准差=0.0054 ✓；RPV=1.3e-5 ✓；网络密度 90.4% ⚠（λ=5e-7 太小，几乎全连接）；换手率 1.61 ⚠；条件数全部 inf/NaN（M<K 原始协方差奇异，预期中）。
- 讨论 Ridge 必要性：GLasso 数学上不需要满秩 Σ，但 sklearn `mode='cd'` 坐标下降求解器内部需要 W₁₁⁻¹/²，奇异矩阵导致 Cholesky 崩。Ridge 是求解器级别的保护措施。
- 创建 `_test_lars.py` 测试 `mode='lars'`（最小角回归）能否在无 Ridge 下处理奇异协方差。待运行对比 CD vs LARS。
- LARS 测试结果：3天仅1天通过（75秒/天），首尾崩盘日同CD一样崩。结论：LARS 不可靠+极慢（250×），Ridge+CD 是唯一稳定方案。已确认不需要换求解器。
- 审查 `特征工程.py`：外生变量是随机生成占位（需后续接入真实数据），邻接矩阵传空字典 `{}`（需从步骤二补充）。
- 发现 Table1 与论文不一致。论文需要4行：Realized GMVP weight(全量合并统计)、Realized variance RPV、Network degree(日平均度)、Turnover。当前多输出了截面极差/网络密度/条件数等无用指标。
- 修改 `步骤二.py`：①加 BLAS 5 行环境变量控制 ②删 cond_val 计算 ③返回 adj 矩阵 ④加逐日邻接矩阵保存(`adjacency/YYYYMMDD.npy`) ⑤Table1 对齐论文4行。
- 纯权重计算终版运行中（~4.8s/天，预计 3h，失败率 <1%）。
- Table1 从已有权重数据重算完成（与原数据一致）。
- 更新 README：§1 当前数据结果、§2.3/§2.10 辅助脚本表对齐实际文件、§2.11 云端脚本改为实际存在的4个、§7 输出文件新增 adjacency/、删除不存在文件的引用（R.R、云端试用脚本等）。已弃用模块新增 4 条。
- 2026-06-25 凌晨: 纯权重计算(λ=5e-7)跑完，2424/2436 成功，12 天 Ridge 退避链耗尽。产出 reg_weights_2436.csv + 2424 天 adjacency/。Table1 用新数据更新。
- 网络密度 91.8% 太密 → 阈值截断无效(1e-3仍 90.8%) → 重选 λ。λ-密度扫描：λ=3e-6 密度 64.2% / 度 251 / OOS 方差仅+5.6% — 帕累托最优。
- 修改 `纯权重计算.py` LAM=3e-6，更新 `λ值.txt`，更新 README §1。
- λ=3e-6 跑完：2425/2436成功(99.5%)。网络度 244(62.4%), RPV=2.15e-05, Turnover=1.53, GMVP均值=0.002551。产 adjacency 2425天。Table1已更新。
