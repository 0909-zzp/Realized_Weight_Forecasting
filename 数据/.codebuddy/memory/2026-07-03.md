# 2026-07-03 工作日志

## 网格搜索完成 & 参数更新
- 验证集最优参数：λ₁=1e-04, λ₃=5e-04, τ=0.9, λ_net=5e-03, λ_s=1e-03
- M4 vs M3 改善 12.2%（网络惩罚有效）
- 外生变量贡献微弱（M3≈M2），原因：GMVP权重自相关性极强，滞后权重已携带足够信息
- Table 4 = 消融分析（拆组件），Table 2 = 水平对比（比模型）

## 文件变更
- `共享模块.py`：更新全部VARX超参数，摘除[TBD]
- `README.md` §5.2-5.3：参数表更新
- `VAR及拓展.py`：docstring注释同步
- `MEMORY.md`：新增VARX已确定参数表和Table2/4分工说明

## 独立网格搜索 & 稀疏度约束
- M3不如M2的8.6%差距是λ₁选择效应（5e-04 vs 1e-04），不是外生变量的错
- M2 λ₁=5e-04 时稀疏度99%，是GMVP低波动偏好导致Lasso把模型压成近似常数权重
- 纯MSE选参数偏好极端稀疏（特征工程几乎全被零化）
- 加入 SPARSITY_CAP=95% 约束，排除>95%稀疏的参数组合
- 约束后M2最优变为λ₁=1e-04（80.8%稀疏），约225个特征存活
- `独立网格搜索.py`：M2-M5各自独立搜索参数，无继承，加95%稀疏上限
- 新增特征存活报告（report_features），搜索完成后输出各模型非零系数→特征名映射
- 外生变量9维：VIX, MarketVol, BA_Spread, TermSpread, CreditSpread, SP500Ret, DXYRet, CS_Skew, CS_Kurt
